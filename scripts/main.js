// main.js
paper.install(window); // Instala Paper.js en el ámbito global
window.onload = function () {
  paper.setup(document.getElementById('myCanvas')); // Inicializa Paper.js con el lienzo

  // Aquí irá la lógica de inicio del juego
}
